// Placeholder: si luego quieres reproducir un CSV/JSON local.
// Por ahora se deja simple para que el proyecto esté completo.
export function replayStub() {
  console.log("Replay listo (stub). Si quieres, lo implementamos con carga de archivo.");
}
